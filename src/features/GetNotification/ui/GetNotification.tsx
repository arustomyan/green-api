import { NotificationBlock } from "shared/ui";

export const GetNotification = () => {
  return (
    <div>
      <NotificationBlock message={""} user="dkcdkcmd" countNotification={3} />
    </div>
  );
};
